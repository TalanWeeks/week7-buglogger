{
  "name": "week7-buglogger",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd week7-buglogger.client && npm i",
    "setup:server": "cd week7-buglogger && npm i",
    "start": "node week7-buglogger/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
